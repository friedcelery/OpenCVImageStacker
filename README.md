# OpenCV Image Stacker
 
A basic no frills image stacker for noise reduction. Uses ORB for feature matching.

Reads all images in the same directory and creates an output folder for the result image. 
